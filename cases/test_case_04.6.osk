
# test_case_04.6a.osk
# user-defined iteration variables

***
numRows = 5
numCols = 7
***

Slabs_array() << Cube() [ {1}                                         # 1st
                             (a = (rowNum*numCols) + colNum)
                             +(-.5, -.5,  0)    # move to center        # Transform
                             *( .05, .1, .1)       # scale to thin slab    # Set
                             @(  0, (a)*(45), 0)   # rotate around y axis
                             @( -45,  0,  0)       # rotate around x axis
                             +( .5,  .5,  0)    # move back to viewport
                        ]
              [ {numRows} +( -.5 + (i*1), 0,  0) ]  # moveX as function(i) # 2nd
                                                                     # Transform Set
              [ {numCols} +(  0, -.5 + (i*1), 0) ]  # moveY as function(i) # 3rd
                                                                     # Transform Set

Film() :: Slabs_array() [ {120} ]



